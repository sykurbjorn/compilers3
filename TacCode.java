/**
 * Created by siddi on 06/11/14.
 */
public enum TacCode {
        LABEL, UMINUS, ASSIGN,
        ADD, SUB, MULT, DIVIDE, DIV, MOD,
        OR, AND, NOT,
        LT, LE, GT, GE, EQ, NE,
        GOTO, CALL,
        APARAM, FPARAM,
        VAR, RETURN, NOOP
}
